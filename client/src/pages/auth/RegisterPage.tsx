import { FormEvent, useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { ArrowRight, UserPlus } from "lucide-react";
import { useAuth } from "../../context/AuthContext";
import { getApiError } from "../../lib/api";
export function RegisterPage(){const {register}=useAuth();const navigate=useNavigate();const [form,setForm]=useState({name:"",email:"",password:""});const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError("");setLoading(true);try{await register(form.name,form.email,form.password);navigate("/dashboard",{replace:true});}catch(err){setError(getApiError(err,"Unable to create account"));}finally{setLoading(false)}}return <div className="auth-page"><div className="auth-visual"><div className="auth-visual-copy"><span className="section-index">DIGITAL HEROES · NEW MEMBER</span><h2>Start with a score.<br/><em>Leave an impact.</em></h2><p>Build your profile, track your game and join a community where every round can contribute to something bigger.</p></div></div><div className="auth-side"><Link to="/" className="auth-brand"><span className="brand-mark">DH</span><strong>Digital Heroes</strong></Link><div className="auth-card"><h1>Create your account</h1><p>Join the community and make your next round count.</p><form className="form" onSubmit={submit}>{error&&<div className="alert error">{error}</div>}<label>Name<input required minLength={2} placeholder="Your full name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Email<input type="email" required placeholder="you@example.com" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Password<input type="password" required minLength={6} placeholder="At least 6 characters" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><button className="btn btn-accent full" disabled={loading}><UserPlus size={17}/>{loading?"Creating account":"Create account"}<ArrowRight size={15}/></button><p className="form-footer">Already registered? <Link to="/login">Sign in</Link></p></form></div></div></div>}
